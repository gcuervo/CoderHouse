val1 === val2

var str2 = "bar";



var str3v= str1 + str2;

Number(str1) - Number(str2);

// me fino si tr1 empieza con "f" o "b"

console.log(str1[0] === "f" || str1[0]=== "b");

/* imprie en la cosola si str1 empieza con una consonante;*/


var  a = 2    // esto es un statement

las variables se inicializan con undefined

cuando se accede en una variable a un campo onde no 
contiene datos da undefined

las variables aunque sean creadas dentro del for tienen un alcance mas amplio

.substring( )

.substr()   // es ua actualizacion del anterior. Arregla problemas
			// de ingreso de datos

Existen arrays que tienen huecos entre sus elementos, estos 
se llaman arrays dispersos

.indexOf();
.lastIndexOf();
.length;
.substr();
.substring();

array.pop()   // saca el ultimo elemento y lo devuelve.

array.shift()  // saca el elemento de la primera posicion y lo devuelve.

array.unshift("cero")  // crea un elemento en la primera posicion y mueve los siguientes.

array.push()		// agregar elementos a lo ultimo

1 in array   // veo si la posicion 1 en el array tiene un hueco o no

